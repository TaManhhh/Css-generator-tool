import type { ITransform } from '~/types/index.type'

export const INITIALTRANSFORM: ITransform = {
    scale: 1,
    rotate: 0,
    translateX: 0,
    translateY: 0,
    skewX: 0,
    skewY: 0,
    transformX: 50,
    transformY: 50,
}
